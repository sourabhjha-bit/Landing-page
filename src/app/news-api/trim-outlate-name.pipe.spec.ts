import { TrimOutlateNAmePipe } from './trim-outlate-name.pipe';

describe('TrimOutlateNAmePipe', () => {
  it('create an instance', () => {
    const pipe = new TrimOutlateNAmePipe();
    expect(pipe).toBeTruthy();
  });
});
